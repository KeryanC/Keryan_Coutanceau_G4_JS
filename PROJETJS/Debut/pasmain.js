/*
var jsp = 4
var jsp2 = 8
if (jsp && jsp2 < 10){
console.log(jsp2+jsp)}
else{
    console.log("flemme")
}

function divide(nbr1, nbr2) {
    return nbr1/nbr2
}
console.log (divide(4, 8))
*/

/* 
const nom = "Didier";
var argent = 100;
var stock = 25;
const prix = 5;

function item(arg, stk, pri) {
let achete = 0;
    while(arg >= pri && stk>0) {
        stk--;
        achete++;
        arg= arg-pri;
        console.log("stock", stk);
        console.log("argent",arg);
        
    }
return nom+ " a acheté " + achete + " produit, il lui reste " + arg + " $ et il reste " + stk + " stock.";

}

console.log (item(argent, stock, prix)); 
*/

var eleve = ["Gua", "Cah", "Moe", "Lle"];
var price = [10, 20, 30, 40, 50];
console.log("prix base " + price)













